# shellcheck shell=sh
# https://direnv.net/man/direnv-stdlib.1.html
PATH_add tools
# shellcheck disable=SC2155
export PROJECT_DIR="$(pwd)"
export SONAR_HOST_URL="http://localhost:9000"
source "${PROJECT_DIR}/.secret"
